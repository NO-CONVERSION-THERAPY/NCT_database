# 在執行本地 Worker 開發之前，複製此檔案並重新命名為 `.dev.vars`：
# cp .env.example .dev.vars
#
# 進行遠端部署時，應在 Cloudflare 的 Variables（變數）/ Secrets（機密）中設定相同的 Key。
# 這些變數刻意不儲存於 `wrangler.toml` 中，如此一來，
# 透過 `wrangler deploy --keep-vars` 進行基於 Git 的部署時，就不會覆蓋掉 Dashboard（控制台）上的線上數值。

# 此 Worker 所需、但「非」透過 `.env` 設定的 Cloudflare 綁定（Bindings）：
# - `DB`: D1 資料庫綁定，於 `wrangler.toml` 中設定
# - `ASSETS`: 靜態資產綁定，由 Wrangler 根據 `[assets]` 設定自動生成
# 啟用 R2 儲存桶時的選填項目：
# - `EXPORT_BUCKET`: 用於排程/管理員匯出資料的 R2 儲存桶綁定
# - `MEDIA_BUCKET`: 用於鏡像（備份）學校媒體物件的 R2 儲存桶綁定
#
# 生產環境路由設定（源自 wrangler.toml）：
#   testdb.medicago.top   -> NCT_database  (即本 Worker)
#   testbk.medicago.top   -> NCT_backend   (子節點/後端)
#   testnct.medicago.top  -> NCT_frontend  (前端)
#
# 本 Worker 不需要設定任何外部 URL 的環境變數；子節點會透過 `/api/sub/report` 
# 註冊其自身的 `SERVICE_PUBLIC_URL`（例如 `https://testbk.medicago.top`），
# 母節點（本 Worker）會從該註冊資訊中推導出每一次的 Push（推送）、Pull（拉取）及 Databack（數據回傳）網址。
# 生產環境部署時，只需在 Cloudflare Dashboard 中設定下方的 Secrets/Vars 即可。
#
# 下方表格依重要性排序。
# 「預設值（Default）」代表省略該變數時的系統執行行為。

# 優先級 1 - 任何安全部署皆屬必填
# | 環境變數 | 預設值 | 用途 |
# | --- | --- | --- |
# | `ENCRYPTION_KEY` | 無 | 用於加密與解密受保護之資料列欄位的必要對稱金鑰。 |
ENCRYPTION_KEY=replace-with-openssl-rand-base64-32

# 優先級 2 - 選填的匯出郵件寄送功能
# | 環境變數 | 預設值 | 用途 |
# | --- | --- | --- |
# | `RESEND_API_KEY` | 空白 | 啟用透過 Resend 服務寄送匯出郵件的功能。 |
# | `EXPORT_EMAIL_TO` | 空白 | 接收匯出郵件的目的地電子信箱地址。 |
# | `EXPORT_EMAIL_FROM` | 空白 | Resend 寄送匯出郵件時顯示的寄件者電子信箱地址。 |
RESEND_API_KEY=
EXPORT_EMAIL_TO=
EXPORT_EMAIL_FROM=

# 優先級 3 - 選填的媒體 R2 容量警報
# | 環境變數 | 預設值 | 用途 |
# | --- | --- | --- |
# | `MEDIA_CAPACITY_ALERT_BYTES` | `8000000000` | 當媒體 R2 儲存桶達到此大小時寄送警報郵件；預設為 8 GB。 |
# | `MEDIA_CAPACITY_ALERT_MIN_INTERVAL_MS` | `86400000` | 當容量持續高於閾值時，重複寄送容量警報郵件的最小間隔時間（毫秒）。 |
# | `MEDIA_CAPACITY_ALERT_EMAIL_TO` | `EXPORT_EMAIL_TO` | 可選項目，用以覆蓋（指定）媒體容量警報的收件者。 |
# | `MEDIA_CAPACITY_ALERT_EMAIL_FROM` | `EXPORT_EMAIL_FROM` | 可選項目，用以覆蓋（指定）媒體容量警報的寄件者。 |
MEDIA_CAPACITY_ALERT_BYTES=8000000000
MEDIA_CAPACITY_ALERT_MIN_INTERVAL_MS=86400000
MEDIA_CAPACITY_ALERT_EMAIL_TO=
MEDIA_CAPACITY_ALERT_EMAIL_FROM=

# 優先級 4 - 選填的命名與效能微調
# | 環境變數 | 預設值 | 用途 |
# | --- | --- | --- |
# | `APP_NAME` | `NCT API SQL` | 用於健康檢查（Health）與匯出回應中，易於閱讀的服務名稱。 |
# | `DEFAULT_ENCRYPT_FIELDS` | `name,phone,email,idCard` | 當資料紀錄未明確指定 `encryptFields` 時，預設對原始 Payload 進行加密的欄位列表（以逗號分隔）。 |
# | `SUB_AUTH_MAX_FAILURES` | `5` | 在母節點將該 Token 綁定的子節點身分列入黑名單前，允許的子節點認證失敗次數。 |
# | `SUB_REPORT_MIN_INTERVAL_MS` | `5000` | 來自同一個已驗證子節點服務，所允許的子節點回報（Sub Report）最小間隔時間（毫秒）。 |
# | `SUB_PULL_BATCH_SIZE` | `10` | 在單次恢復拉取（Recovery Pull）執行中，所處理的已註冊子節點最大數量。 |
# | `SUB_PULL_MAX_ATTEMPTS` | `5` | 每個「母節點對子節點」的數據回傳匯出請求（Databack Export Request）的最大連續重試次數。 |
# | `SUB_PULL_RECORD_LIMIT` | `100` | 在每次拉取分頁中，向子節點請求數據回傳的最大資料列數。 |
# | `SUB_PULL_RETRY_DELAY_MS` | `60000` | 「母節點對子節點」數據回傳匯出請求失敗後的重試延遲時間（毫秒）。 |
# | `SUB_PULL_TIMEOUT_MS` | `10000` | 每次「母節點對子節點」恢復拉取請求的超時時間（毫秒）。 |
APP_NAME="NCT API SQL"
DEFAULT_ENCRYPT_FIELDS=name,phone,email,idCard
SUB_AUTH_MAX_FAILURES=5
SUB_REPORT_MIN_INTERVAL_MS=5000
SUB_PULL_BATCH_SIZE=10
SUB_PULL_MAX_ATTEMPTS=5
SUB_PULL_RECORD_LIMIT=100
SUB_PULL_RETRY_DELAY_MS=60000
SUB_PULL_TIMEOUT_MS=10000